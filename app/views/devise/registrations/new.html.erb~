<div class="row">
	<div class="small-6 columns">
		<%= form_for(resource, :as => resource_name, :url => registration_path(resource_name)) do |f| %>
  		<%= devise_error_messages! %>
  		<fieldset class="round" style="background-color: #FFFFFF";>
  			<legend>Sign up</legend>
  			<div class="small-3 columns">
  				<%= f.label :email, :for => "right-label", :class => "right inline" %>
  			</div>
  			<div class="small-9 columns">
  				<%= f.email_field :email, :autofocus => true, :id => "right-label" %>
  			</div>
  			
  			<div class="small-3 columns">
  				<%= f.label :password, :for => "right-label", :class => "right inline" %>
  			</div>
  			<div class="small-9 columns">
  				<%= f.password_field :password, :id => "right-label" %>
  			</div>
  			
  			<div class="small-3 columns">
  				<%= f.label :password_confirmation, :for => "right-label", :class => "right inline" %>
  			</div>
  			<div class="small-9 columns">
  				<%= f.password_field :password_confirmation, :id => "right-label" %>
  			</div>

  			<div><%= f.submit "Sign up", :class => "tiny button radius" %></div>
<% end %>

<%= render "devise/shared/links" %>
  		</fieldset>

  
	</div>
</div>


